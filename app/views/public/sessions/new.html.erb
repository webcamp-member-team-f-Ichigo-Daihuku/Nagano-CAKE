<div class="container">
  <div class="row">
    <div class="col">
      <h4 class="my-5">ログイン</h4>
      <h6>会員の方はこちらからログイン</h6>
      <table class="table table-borderless">
        <%= form_with model: @public, url: public_session_path do |f| %>
        <tr>
          <td>メールアドレス</td>
          <td><%= f.text_field :email, autofocus: true, autocomplete: "email" %></td>
          <td></td>
        </tr>

        <tr>
          <td>パスワード</td>
          <td><%= f.password_field :password, autocomplete: "current-password" %></td>
          <td></td>
        </tr>

        <tr>
          <td></td>
          <td><%= f.submit "ログイン", class:"btn btn-success" %></td>
          <td><%= link_to "退会する", public_publicers_check_path(@publicer), class:"btn btn-danger" %></td>
        </tr>

        <% end %>
      </table>
      <h4 class="my-5">会員登録がお済みでない方</h4>
      <table>
        <tr>
          <td><%= link_to "こちら", new_public_registration_path %>　から新規登録を行ってください。</td>
        </tr>
      </table>
    </div>
  </div>
</div>